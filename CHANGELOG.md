# Changelog

## 0.1.0

- Extracted the browser-first Airpoint SDK as a standalone monolithic package.
- Kept the public plugin entrypoint, manifest helpers, asset helpers, runtime assets, and premium asset tooling in one repo.
- Added a minimal browser example that consumes `@airpoint/sdk` through the public plugin API.